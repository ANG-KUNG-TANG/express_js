export const TaskStatus = Object.freeze({
    PENDING: "pending",
    IN_PROGRESS: "in_progress",
    COMPLETED: 'completed'
})

export const TaskPriority = Object.freeze({
    LOW:'low',
    MEDIUM:'medium',
    HIGH:'high',
})